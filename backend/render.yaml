services:
  - type: web
    name: servicedesk-backend
    env: python
    buildCommand: |
      pip install -r backend/requirements.txt
      cd backend && python manage.py migrate && python manage.py collectstatic --noinput
    startCommand: cd backend && gunicorn backend.wsgi:application --bind 0.0.0.0:8000
    envVars:
      - key: DJANGO_DEBUG
        value: "False"
      # Эти переменные заполнишь в панели Render:
      # - key: DJANGO_SECRET_KEY
      #   generateValue: true
      # - key: DJANGO_ALLOWED_HOSTS
      #   value: "your-service.onrender.com"
      # - key: CORS_ALLOWED_ORIGINS
      #   value: "https://your-frontend.vercel.app"

