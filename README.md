# ServiceDesk - Система управления заявками

Полнофункциональное веб-приложение для управления заявками с разделением на фронтенд (Next.js) и бэкенд (Django).

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Настройка базы данных](#настройка-базы-данных)
- [Запуск проекта](#запуск-проекта)
- [Структура проекта](#структура-проекта)
- [API Endpoints](#api-endpoints)
- [Создание тестового пользователя](#создание-тестового-пользователя)

## 🔧 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Python 3.10+** (для бэкенда)
- **Node.js 18+** и **npm** (для фронтенда)
- **PostgreSQL 12+** (база данных)
- **Git** (для клонирования репозитория)

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <url-репозитория>
cd ServiceDesk
```

### 2. Настройка бэкенда (Django)

#### 2.1. Переход в директорию бэкенда

```bash
cd backend/backend
```

#### 2.2. Создание виртуального окружения

**Windows:**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

#### 2.3. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 2.4. Настройка базы данных

Откройте файл `backend/backend/backend/settings.py` и настройте параметры подключения к PostgreSQL:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'your_database_name',      # Имя вашей базы данных
        'USER': 'your_username',            # Имя пользователя PostgreSQL
        'PASSWORD': 'your_password',        # Пароль PostgreSQL
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

#### 2.5. Создание базы данных в PostgreSQL

```sql
CREATE DATABASE your_database_name;
```

#### 2.6. Применение миграций

```bash
python manage.py migrate
```

#### 2.7. Создание суперпользователя (опционально, для доступа к админке)

```bash
python manage.py createsuperuser
```

### 3. Настройка фронтенда (Next.js)

#### 3.1. Переход в директорию фронтенда

```bash
cd ../../frontend
```

#### 3.2. Установка зависимостей

```bash
npm install
```

## 🗄️ Настройка базы данных

### Создание тестовых данных

После применения миграций вы можете создать тестовые данные через Django admin или через Python shell:

```bash
cd backend/backend
python manage.py shell
```

В Python shell:

```python
from back.models import User, Office

# Создание офиса
office = Office.objects.create(
    name='Главный офис',
    region='Свердловская область',
    city='Екатеринбург',
    address='г. Екатеринбург, ул. Мира, д. 19',
    level=1
)

# Создание пользователя
user = User(
    email='user@example.com',
    username='testuser',
    first_name='Иван',
    last_name='Иванов',
    middle_name='Иванович',
    position='Разработчик',
    role='employee',
    office=office,
    desk_number='19',
    birth_date='2000-01-01'  # Формат: YYYY-MM-DD
)
user.set_password('password123')
user.save()
```

## 🚀 Запуск проекта

### Запуск бэкенда

Откройте первый терминал:

```bash
cd backend/backend
.\venv\Scripts\Activate.ps1  # Windows
# или
source venv/bin/activate      # Linux/Mac

python manage.py runserver
```

Бэкенд будет доступен по адресу: `http://127.0.0.1:8000`

### Запуск фронтенда

Откройте второй терминал:

```bash
cd frontend
npm run dev
```

Фронтенд будет доступен по адресу: `http://localhost:3000`

### Доступ к админ-панели Django

После создания суперпользователя:
- URL: `http://127.0.0.1:8000/admin`
- Используйте учетные данные суперпользователя

## 📁 Структура проекта

```
ServiceDesk/
├── backend/                 # Django бэкенд
│   └── backend/
│       ├── back/           # Основное приложение
│       │   ├── models.py   # Модели данных
│       │   ├── views.py    # API endpoints
│       │   └── admin.py    # Настройки админки
│       ├── backend/        # Настройки проекта
│       │   ├── settings.py # Конфигурация
│       │   └── urls.py     # URL маршруты
│       ├── manage.py
│       └── requirements.txt
│
└── frontend/               # Next.js фронтенд
    └── src/
        ├── app/            # Страницы и API routes
        ├── components/     # React компоненты
        └── contexts/       # React контексты
```

## 🔌 API Endpoints

### Аутентификация

- **POST** `/api/auth/login/` - Вход в систему
  ```json
  {
    "email": "user@example.com",
    "password": "password123"
  }
  ```

### Пользователь

- **GET** `/api/user/profile/<user_id>/` - Получение профиля пользователя

## 👤 Создание тестового пользователя

### Через Django Admin

1. Запустите сервер: `python manage.py runserver`
2. Откройте `http://127.0.0.1:8000/admin`
3. Войдите с учетными данными суперпользователя
4. Перейдите в раздел "Users"
5. Нажмите "Add User"
6. Заполните все поля:
   - Email
   - Username
   - Password (будет автоматически захеширован)
   - First name, Last name, Middle name
   - Position, Role
   - Desk number
   - Birth date
   - Office (создайте офис заранее, если его нет)

### Через Python shell

См. раздел [Создание тестовых данных](#создание-тестовых-данных)

## 🔐 Тестовые учетные данные

После создания пользователя используйте:
- **Email**: `user@example.com` (или тот, который вы указали)
- **Password**: `password123` (или тот, который вы указали)

## ⚙️ Настройки CORS

CORS уже настроен для работы с фронтендом на порту 3000. Если нужно изменить порт, отредактируйте `backend/backend/backend/settings.py`:

```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]
```

## 🐛 Решение проблем

### Ошибка подключения к базе данных

- Убедитесь, что PostgreSQL запущен
- Проверьте правильность настроек в `settings.py`
- Убедитесь, что база данных создана

### Ошибка миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### Ошибка CORS

- Проверьте, что бэкенд запущен на порту 8000
- Проверьте настройки CORS в `settings.py`
- Убедитесь, что фронтенд обращается к правильному URL

### Проблемы с зависимостями

**Бэкенд:**
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**Фронтенд:**
```bash
rm -rf node_modules package-lock.json
npm install
```

## 📝 Дополнительная информация

- Django версия: 5.2.7
- Next.js версия: 16.0.0
- React версия: 19.2.0
- PostgreSQL: требуется версия 12+

## 👥 Разработчики

Для вопросов и предложений создавайте issues в репозитории проекта.

---

**Удачной разработки! 🚀**

